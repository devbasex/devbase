[project]
name = "devbase"
version = "2.2.0"
description = "Docker-based Development Environment Manager"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
    "pyrage>=1.2",
]

[project.optional-dependencies]
# `devbase env export/import` の S3 backend (`s3://`) を使う場合のみ必要
s3 = [
    "boto3>=1.34",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["lib"]

[tool.uv.sources]
